# Files that must NOT be uploaded as public, downloadable assets.
# _worker.js holds the Payfast credentials and signing logic — it runs
# server-side only and must never be reachable in a browser.
_worker.js
wrangler.jsonc
.assetsignore
.gitignore
node_modules
.git
.wrangler
